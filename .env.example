DB_NAME=fintrack
DB_USER=fintrack
DB_PASSWORD=changeme
DB_HOST=db
DB_PORT=5432
DJANGO_SECRET_KEY=change-me-to-a-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
REDIS_URL=redis://redis:6379/0

# Google OAuth2 (optional — leave empty to disable Google login)
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Type: Web application — Authorized JavaScript origins: http://localhost:5173
GOOGLE_CLIENT_ID=

# Set to false to disable public registration (admin creates users via Django admin)
ALLOW_REGISTRATION=true
